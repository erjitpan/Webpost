---
-  hosts: all
   tasks:
     - include_vars: vars.yml
       vars:
         webpost_file: "{{ webpost_file }}"
- name: Include the base role
    include_role:
      name: webpost.yml
      role_vars: "{{ webpost_file }}"

   
